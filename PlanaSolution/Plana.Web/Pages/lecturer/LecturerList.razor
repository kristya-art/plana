@page "/"
@inherits LecturerListBase
<h3>LecturerList</h3>
<input type="checkbox" id="showFooter" @bind="ShowFooter"/>
<label for="showFooter">Show Footer</label>

@if (Lecturers == null)
{
    <div class="spinner"></div>
}
else { 
<div class="card-deck">
    @foreach (var lecturer in Lecturers)
    { 
    @*<div class="card m-3" style="min-width: 18rem; max-width:30.5%;">
        <div class="card-header">
            <h3>@lecturer.FirstName @lecturer.LastName</h3>

        </div>
        <img class="card-img-top imageThumbnail" src="@lecturer.PhotoPath"/>
        <div class="card-footer text-center">
            <a href="@($"lecturerdetails/{lecturer.LecturerId}")" class="btn btn-primary m-1">View</a>
            <a href="#" class="btn btn-primary m-1">Edit</a>
            <a href="#" class="btn btn-danger m-1">Delete</a>
        </div>

    </div>*@
        <DisplayLecturer Lecturer="lecturer" ShowFooter="ShowFooter"
                         
                         OnLecturerDeleted="LecturersDeleted">

        </DisplayLecturer>
    
    }

        
    </div>
}